# Managed by majestic. Comments and formatting are removed when settings change
# via the web UI or API. Only settings that differ from this build's defaults
# are kept, so a default that changes in a later version still reaches you.
image:
  rotate: 0
video0:
  fps: 20
  enabled: true
  gopSize: "1"
  size: 2880x1620
video1:
  fps: 15
  size: 704x576
  enabled: false
jpeg:
  enabled: false
audio:
  enabled: false
  volume: 30
  outputVolume: 30
watchdog:
  enabled: false
hls:
  enabled: false
# 本板未接 IRCUT 电磁线圈（原厂固件 IrCutOpen=-1/IrCutClose=-1 可证），
# 关闭 "Drive the IR-cut filter"，否则 Web UI 持续报 no-pins 警告。
nightMode:
  irCutEnabled: false
